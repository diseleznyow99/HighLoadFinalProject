apiVersion: v1
kind: Service
metadata:
  name: highload-service
  labels:
    app: highload-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: highload-service

